name: 5

on:
  workflow_dispatch:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: set up zulu JDK 17 
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'zulu'
